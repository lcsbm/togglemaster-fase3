apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: analytics-service-secret
  namespace: togglemaster
spec:
  refreshInterval: "0"
  secretStoreRef:
    name: aws-secrets-manager
    kind: ClusterSecretStore
  target:
    name: analytics-service-secret
    creationPolicy: Owner
  dataFrom:
    - extract:
        key: togglemaster/analytics-service
