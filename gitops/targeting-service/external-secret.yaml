apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: targeting-service-secret
  namespace: togglemaster
spec:
  refreshInterval: "0"
  secretStoreRef:
    name: aws-secrets-manager
    kind: ClusterSecretStore
  target:
    name: targeting-service-secret
    creationPolicy: Owner
  dataFrom:
    - extract:
        key: togglemaster/targeting-service
